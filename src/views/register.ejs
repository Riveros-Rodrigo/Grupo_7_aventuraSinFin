<!DOCTYPE html>
<html lang="en">
    <!-- head -->
    <%- include('./partials/head',{
        title : 'Register'
    }) %>

<body>
    <!-- header -->
    <%- include('./partials/header') %>
    
    <!-- cuerpo de pagina -->
    <main class="main__register">
        <div class="caja__container">
            <div class="div__container">
                <h1>Creá tu cuenta</h1>
                <form method="post">
                    <div class="div__form">
                        <!-- nombre -->
                        <label for="name">Nombre</label>
                        <input type="text" class="form-control <%= locals.errors ? (errors.name ? 'is-invalid' : 'is-valid') : '' %>" id="name" name="name" value="<%= locals.old && (old.name && old.name) %>">
                        <small class="text-warning fs-6"> <%= locals.errors && (errors.name && errors.name.msg) %> </small>
                        <!-- apellido -->
                        <label for="apellido">Apellido</label>
                        <input type="text" class="form-control <%= locals.errors ? (errors.surname ? 'is-invalid' : 'is-valid') : '' %>" id="apellido" name="surname" value="<%= locals.old && (old.name && old.surname) %>">
                        <small class="text-warning fs-6"> <%= locals.errors && (errors.surname && errors.surname.msg) %> </small>
                        <!-- correo -->
                        <label for="email">Correo</label>
                        <input type="email" class="form-control <%= locals.errors ? (errors.email ? 'is-invalid' : 'is-valid') : '' %>" id="email" name="email" value="<%= locals.old && (old.email && old.email) %>">
                        <small class="text-warning fs-6"> <%= locals.errors && (errors.email && errors.email.msg) %> </small>
                        <!-- contraseña -->
                        <label for="password">Contraseña</label>
                        <input type="password" class="form-control <%= locals.errors ? (errors.password ? 'is-invalid' : 'is-valid') : '' %>" value="<%= locals.old && (old.password && old.password) %>" id="password" type="password" name="password">
                        <small class="text-warning fs-6"> <%= locals.errors && (errors.password && errors.password.msg) %> </small>
                        <!-- contraseña2 -->
                        <label for="password2">Confirmar Contraseña</label>
                        <input type="password" class="form-control <%= locals.errors ? (errors.password2 ? 'is-invalid' : 'is-valid') : '' %>" id="password2" type="password2" name="password2" value="<%= locals.old && (old.password2 && old.password2) %>">
                        <small class="text-warning fs-6"> <%= locals.errors && (errors.password2 && errors.password2.msg) %> </small>
                    </div>
                    <div class="boton__registro">
                        <button type="submit">Registrate</button>
                    </div>
                </form>
                <div class="registrer__redsocial">
                    <p>Al registrarte aceptas los <a href="">Terminos y condiciones</a></p>
                    <h4>¡Chau contraseña! Ingresa con un solo paso.</h4>
                    <ul>
                        <li><i class="fab fa-facebook"></i><a href="">Ingresa con Facebook</a></li>
                        <li><i class="fab fa-google"></i><a href="">Ingresa con Google</a></li>
                    </ul>
                </div>
                <div class="cuenta">
                    <p>¿Ya tenés cuenta?</p>
                    <button><a href="/users/login">Ingresar</a></button>
                </div>
            </div>
        </div>
    </main>
<!-- footer -->
<%- include('partials/footer') %>
<!-- scripts-->
<%- include('./partials/scripts') %>
</body>
</html>